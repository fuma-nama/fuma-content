{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".content/**", ".next/**", "!.next/cache/**", "dist/**"]
    },
    "types:check": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
